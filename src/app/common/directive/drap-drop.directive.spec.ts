import { DrapDropDirective } from './drap-drop.directive';

describe('DrapDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DrapDropDirective(null);
    expect(directive).toBeTruthy();
  });
});
